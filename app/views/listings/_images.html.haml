= stylesheet_link_tag "app/assets/stylesheets/images.scss"

.container
  - @listing.images.each_slice(3) do |image_column|
    .row.text-center
      - image_column.each_with_index do |image, index|
        .col-lg-3.col-md-4.col-6
          - if image
            -# = image_tag(image.url, size: "300x200")
            .img-wrap
              - if current_customer
                = link_to listing_image_path(@listing, index), :method => :delete, data: { confirm: "Are you sure you want to delete this image?" } do
                  %span.close ×
              %img.img-thumbnail{:alt => "", :src => image.url}/