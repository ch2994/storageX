%h1 Bookings #{@booking.listing.name}
%table#bookings.table.table-striped.col-md-12{id:'booking_table'}
  %thead
    %th Customer
    %th Rating
    %th Comments
  %tbody
    -@all_reviews.each do |review|
      %tr
        -Review::index_display_cols.each do |col|
          %td= review[col]



%h4 Your review is very important to us, Thank you!

= form_tag review_path do
  .actions
    =  link_to 'Write a Review', new_review_path(:id => @listing)
